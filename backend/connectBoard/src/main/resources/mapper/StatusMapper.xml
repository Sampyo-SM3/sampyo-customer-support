<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.connectBoard.repository.StatusRepository">

	<!--접수 상태 List-->
    <select id="getAllStatuses" resultType="com.example.connectBoard.dto.StatusDTO">   
		SELECT 
			codeId, 
			codeName, 
			codeDesc, 
			category, 
			parentCodeId, 
			useYn, 
			insertDt, 
			updateDt, 
			orderNum 
		FROM code_master 
		ORDER BY orderNum;
    </select>

</mapper>

